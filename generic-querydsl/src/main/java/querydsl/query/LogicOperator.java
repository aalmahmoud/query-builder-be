package querydsl.query;

/**
 * Boolean operator used to combine the conditions and nested groups of a
 * {@link QueryGroup} (and of the top-level {@link QueryRequest}).
 */
public enum LogicOperator {
    AND,
    OR
}
